<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="#fff">
    <RelativeLayout
        android:layout_width="match_parent"

        android:layout_marginTop="0dp"

        android:id="@+id/ordered_drink_top_panel"
        android:layout_height="100dp">

        <TextView
            android:layout_alignParentTop="true"
            android:id="@+id/textView_ordered_drink"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fontFamily="@font/sriracha"
            android:background="@color/colorAccent"
            android:gravity="center"
            android:text="Đồ uống đã gọi"
            android:textColor="#fff"
            android:textSize="40sp"
            tools:ignore="MissingConstraints"></TextView>
        <ImageButton
            android:id="@+id/img_back_btn_from_kitchen_ordered_drink"
            android:layout_width="50dp"
            android:scaleType="centerInside"
            android:layout_marginLeft="10dp"
            android:layout_centerVertical="true"
            android:background="@color/colorAccent"
            android:src="@drawable/white_arr2"
            android:layout_height="30dp"></ImageButton>
    </RelativeLayout>
    <ListView
        android:id="@+id/ordered_drinklistview"
        android:layout_width="match_parent"
        android:padding="5dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/ordered_drink_top_panel"
        tools:listitem="@layout/item_kitchen_ordered_food"></ListView>

</RelativeLayout>