<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logosmred"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        >
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"></activity>
        <activity android:name=".activities.AddFood"/>
        <activity android:name=".activities.NewFood"/>
        <activity android:name=".activities.Payment"/>
        <activity android:name=".activities.KitchenFood"></activity>
        <activity android:name=".activities.PayBill"/>
        <activity android:name=".activities.Order"/>
        <activity android:name=".activities.WaiterFoodReady"/>
        <activity android:name=".activities.KitchenDrink"/>
        <activity android:name=".activities.ChooseFood"/>
        <activity android:name=".activities.ATest"/>

    </application>


</manifest>